"private fun TimelineActivityCard(\n\tactivity: ProfileActivityItem,\n\tuserDisplayName: String,\n\tuserAvatarEmoji: String,\n\tuserProfileImageUrl: String,\n\tonLikeClick: (ProfileActivityItem) -> Unit,\n\tonCommentClick: (ProfileActivityItem, String) -> Unit,\n\tonPinClick: (ProfileActivityItem, Boolean) -> Unit,\n\tonHideClick: (ProfileActivityItem) -> Unit,\n\tonDeleteClick: (ProfileActivityItem) -> Unit,\n\tonShareClick: (ProfileActivityItem) -> Unit,\n\tonVisibilityClick: (ProfileActivityItem, com.example.kampus.ui.feed.PostItem.PostVisibility) -> Unit,\n) {\n\tvar showMenu by remember { mutableStateOf(false) }\n\tvar showComments by remember { mutableStateOf(false) }\n\tvar commentInputText by remember { mutableStateOf(\"\") }\n\tval context = LocalContext.current\n\n\tColumn(\n\t\tmodifier = Modifier\n\t\t\t.fillMaxWidth()\n\t\t\t.clip(RoundedCornerShape(22.dp))\n\t\t\t.background(Card)\n\t\t\t.border(1.dp, if (activity.isPinned) Blue.copy(alpha = 0.5f) else Border, RoundedCornerShape(22.dp)),\n\t\tverticalArrangement = Arrangement.spacedBy(0.dp)\n\t) {\n\t\tif (activity.type != \"create_event\") {\n\t\t\t// Header row for non-event cards\n\t\t\tRow(\n\t\t\t\tmodifier = Modifier\n\t\t\t\t\t.fillMaxWidth()\n\t\t\t\t\t.padding(start = 14.dp, top = 14.dp, end = 14.dp, bottom = 4.dp),\n\t\t\t\tverticalAlignment = Alignment.CenterVertically,\n\t\t\t\thorizontalArrangement = Arrangement.spacedBy(10.dp)\n\t\t\t) {\n\t\t\t\t// Profile image / avatar\n\t\t\t\tif (userProfileImageUrl.isNotBlank()) {\n\t\t\t\t\tAsyncImage(\n\t\t\t\t\t\tmodel = userProfileImageUrl,\n\t\t\t\t\t\tcontentDescription = null,\n\t\t\t\t\t\tcontentScale = ContentScale.Crop,\n\t\t\t\t\t\tmodifier = Modifier\n\t\t\t\t\t\t\t.size(36.dp)\n\t\t\t\t\t\t\t.clip(CircleShape)\n\t\t\t\t\t)\n\t\t\t\t} else {\n\t\t\t\t\tBox(\n\t\t\t\t\t\tmodifier = Modifier\n\t\t\t\t\t\t\t.size(36.dp)\n\t\t\t\t\t\t\t.clip(CircleShape)\n\t\t\t\t\t\t\t.background(Color.White.copy(alpha = 0.1f)),\n\t\t\t\t\t\tcontentAlignment = Alignment.Center\n\t\t\t\t\t) {\n\t\t\t\t\t\
<truncated 37641 bytes>